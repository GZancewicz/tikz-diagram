\documentclass[tikz,border=12pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}
\usetikzlibrary{shapes.geometric,arrows.meta,positioning,fit,backgrounds,calc}

\definecolor{ink}{HTML}{2E3440}
\definecolor{svc}{HTML}{DCE6F2}
\definecolor{svcline}{HTML}{4C6A92}
\definecolor{store}{HTML}{E5EFE1}
\definecolor{storeline}{HTML}{5B8C5A}
\definecolor{ext}{HTML}{F3E9DC}
\definecolor{extline}{HTML}{A8763E}
\definecolor{async}{HTML}{6C757D}
\definecolor{retry}{HTML}{C0392B}
\definecolor{vpcfill}{HTML}{F7F9FC}

\begin{document}
\begin{tikzpicture}[
  font=\small, color=ink,
  node distance=12mm and 18mm,
  box/.style={draw=svcline, fill=svc, rounded corners=2pt, minimum width=24mm, minimum height=10mm, align=center, line width=0.7pt},
  extbox/.style={draw=extline, fill=ext, rounded corners=2pt, minimum width=24mm, minimum height=10mm, align=center, line width=0.7pt},
  db/.style={cylinder, shape border rotate=90, aspect=0.3, draw=storeline, fill=store, minimum width=20mm, minimum height=13mm, align=center, line width=0.7pt},
  topic/.style={draw=storeline, fill=store, minimum width=26mm, minimum height=10mm, align=center, line width=0.7pt, double, double distance=1.5pt},
  sync/.style={-{Stealth[length=2.5mm]}, line width=0.8pt, color=svcline},
  msg/.style={-{Stealth[length=2.5mm]}, line width=0.8pt, dashed, color=async},
  retryarrow/.style={-{Stealth[length=2.5mm]}, line width=0.9pt, color=retry},
  lbl/.style={font=\scriptsize, fill=white, inner sep=1.5pt, text=ink},
]
% nodes
\node[extbox] (browser) {Browser};
\node[box, right=of browser] (gw) {API Gateway};
\node[box, right=of gw] (orders) {Orders\\service};
\node[db, below=of orders] (pg) {Postgres};
\node[topic, right=of orders] (kafka) {Kafka\\\texttt{orders.created}};
\node[box, right=of kafka] (w2) {worker-2};
\node[box, above=6mm of w2] (w1) {worker-1};
\node[box, below=6mm of w2] (w3) {worker-3};
\node[extbox, right=of w2] (pay) {Payments API\\(3rd party)};

% synchronous edges
\draw[sync] (browser) -- node[lbl,above]{HTTPS} (gw);
\draw[sync] (gw) -- node[lbl,above]{gRPC} node[lbl,below]{p95 40\,ms} (orders);
\draw[sync] (orders) -- node[lbl,right]{INSERT} (pg);
\draw[sync] (w1.east) -- (pay.west);
\draw[sync] (w2.east) -- node[lbl,above]{p95 120\,ms} (pay.west);
\draw[sync] (w3.east) -- (pay.west);

% async edges
\draw[msg] (orders) -- node[lbl,above]{publish} (kafka);
\foreach \w in {w1,w2,w3} \draw[msg] (kafka.east) -- (\w.west);

% retry loop
\draw[retryarrow] (w3.south) to[out=-90,in=-90,looseness=1.3]
  node[lbl, below, text=retry] (retrylbl) {retry $\times$3, then dead-letter} (kafka.south);

% trust boundary, drawn behind everything
\begin{scope}[on background layer]
  \node[draw=svcline, dashed, rounded corners=6pt, fill=vpcfill, inner sep=6mm,
        fit=(gw)(orders)(pg)(kafka)(w1)(w3)(retrylbl),
        label={[anchor=north west, font=\scriptsize\bfseries, color=svcline, inner sep=2mm]north west:Our VPC}] (vpc) {};
\end{scope}

% legend
\begin{scope}[shift={($(vpc.south west)+(0,-6mm)$)}, font=\scriptsize]
  \draw[sync] (0,0) -- (8mm,0) node[right, color=ink]{synchronous call};
  \draw[msg] (0,-4mm) -- (8mm,-4mm) node[right, color=ink]{async message};
  \draw[retryarrow] (0,-8mm) -- (8mm,-8mm) node[right, color=ink]{retry path};
  \draw[dashed, svcline, rounded corners=2pt] (36mm,1.8mm) rectangle (44mm,-1.8mm);
  \node[right, color=ink] at (44mm,0) {trust boundary};
  \node[extbox, minimum width=8mm, minimum height=3.6mm, anchor=west] at (36mm,-6mm) {};
  \node[right, color=ink] at (44mm,-6mm) {outside our control};
\end{scope}
\end{tikzpicture}
\end{document}
